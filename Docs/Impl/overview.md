Unity2022.3.22f1 以降をターゲットとする曲率マップの高速ベイクのUnity拡張機能

対象メッシュに対して曲率マップをベイクする。

入力はメッシュ（複数入力対応）
出力はテクスチャ
- 対象メッシュのマテリアルにベーステクスチャがあればそのフォルダに BakedCurvature/ を用意し、そこに保存する。
- なければプロジェクトルートに BakedCurvature/ を作成して保存する